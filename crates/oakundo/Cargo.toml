[package]
name = "oakundo"
version = "0.1.0"
edition = "2021"
description = "Oak Video Editor undo/redo history module (Rust)"
license = "GPL-3.0-or-later"

[lib]
crate-type = ["rlib"]

[dependencies]
thiserror = "2"
